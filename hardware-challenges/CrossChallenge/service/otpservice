service otpservice
{
    disable 	= no
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = otp
    bind        = 0.0.0.0
    server      = /home/otp/otp
    type        = UNLISTED
    port        = 31337
}
